import { app } from "./app";

app.listen(3333)

// Models estão relacionadas ao BD, já entities não
// Services faz a comunicação com o BD
// Controller é uma camada externa
// DTO é o formato dos dados para comunicação entre camadas
// Providers refere-se à comunicação com uma APi externa
